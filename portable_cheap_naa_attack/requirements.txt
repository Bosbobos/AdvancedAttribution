# Verified against the local `IAD` environment used for smoke tests.
# On a CUDA server, prefer installing torch/torchvision from the official
# PyTorch CUDA index instead of relying on a generic wheel resolution.

numpy==1.25.2
matplotlib==3.10.8
Pillow==12.1.0
scipy==1.16.3
ultralytics==8.3.233
notebook==7.4.5
jupyterlab==4.4.7
ipykernel==6.30.1
torch==2.9.1
torchvision==0.24.1
